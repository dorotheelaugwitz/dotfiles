{{- $machine := promptStringOnce . "machine" "Machine type (work or personal)" "personal" -}}
[data]
    machine = {{ $machine | quote }}

# Auto-commit and push source changes (needs push credentials).
[git]
    autoCommit = true
    autoPush = true
